(ns clojuremagick.file)

; First, let's load resource files for tests only for dev/test environments:
; http://stackoverflow.com/questions/8009829/resources-in-clojure-applications

; Here we open a file, make sure it's in the way clojuremagick expects.
